'use client';

export default function RouterConfigPage() {
  return (
    <div style={{ padding: 24, maxWidth: 800 }}>
      <h1 style={{ fontSize: 22, fontWeight: 700, marginBottom: 16 }}>LiteLLM Router</h1>
      <p style={{ color: 'var(--text-secondary)' }}>
        Router configuration page coming soon. This will show available models, proxy health status,
        and failover settings.
      </p>
    </div>
  );
}
