/**
 * Shared constants for the agent-worker runtime layer.
 */

/** Default maximum number of concurrent terminal PTY sessions. */
export const DEFAULT_MAX_TERMINALS = 5;

/** Default terminal column count when not specified by the caller. */
export const DEFAULT_TERMINAL_COLS = 120;

/** Default terminal row count when not specified by the caller. */
export const DEFAULT_TERMINAL_ROWS = 30;
